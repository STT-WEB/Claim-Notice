# STT CLAIM — ระบบงานเคลม / ตรวจรับ (คู่มือโปรเจกต์)

> อ่านไฟล์นี้ก่อนเริ่มงานทุกครั้ง · เบียร์เรียก Claude ว่า **Candy** · เบียร์ = sasipa@suteetankers.com
> **เวอร์ชันจริงอยู่บรรทัดแรกของ `deploy/CHANGELOG.md` เสมอ** ห้ามเชื่อเลขที่อื่น

---

## ⛔ 6 ข้อห้ามเด็ดขาด

1. **ห้ามแตะ STT NOVA** — คนละโฟลเดอร์ คนละ git คนละ clasp คนละ .bat
   (NOVA อยู่โฟลเดอร์ `AI ระบบงานใหญ่ตาม Job ของจัดซื้อ และ คลังสินค้า` ข้าง ๆ กัน)
2. **อ่านข้อมูลกลางได้ แต่ห้ามเขียนลงไฟล์ของ NOVA แม้แต่ช่องเดียว**
   (`_tests/syntax-check.js` ตรวจให้อัตโนมัติ push ไม่ผ่านถ้าเผลอเขียน)
3. **ห้ามแก้ไฟล์ในคลาวด์** — แก้บนเครื่องเบียร์ผ่าน device bash เท่านั้น
4. **ห้ามบอกว่า deploy สำเร็จ จนกว่าจะเห็นป้ายเวอร์ชันในแอปเปลี่ยนจริง**
5. **`.bat` ต้องเป็น ASCII ล้วนและเป็น CRLF** (ไทยใน .bat ทำ cmd พัง · ชื่อไฟล์ไทยได้)
6. **ห้ามออกแบบฟอร์มที่พึ่งพื้นสี** — เบราว์เซอร์ไม่พิมพ์พื้นหลังโดยค่าเริ่มต้น ต้องใช้เส้นกรอบ+ตัวอักษร
   วิธีตรวจ: สั่งพิมพ์ PDF 2 แบบ (เปิด/ปิดพิมพ์พื้นหลัง) แล้วเทียบ ต้องเหมือนกัน

## ⚠️ กับดักที่เคยกินเวลาไปแล้ว (อ่านก่อนแตะ deploy)
- **`clasp create` เขียนทับ `appsscript.json` ในเครื่องด้วยตัวเปล่าของ Google**
  → โปรเจกต์กลายเป็น "ไม่ใช่เว็บแอป" เปิด `/exec` ไม่ได้ แต่ clasp ยังขึ้น `Deployed @N` ปกติ
  → ชุดทดสอบ `guard-v0100.js` กติกา 8 ตรวจให้แล้ว **push ไม่ผ่านถ้า appsscript.json ไม่ครบ**
- **`clasp deploy` ต้องใส่ `-i <deployment id>` เสมอ** ไม่งั้นได้ URL ใหม่ทุกครั้ง
- **`clasp login --status` ใช้ไม่ได้กับ clasp รุ่นใหม่**
- **git บน Windows ไม่มีชื่อผู้เขียน** → .bat ตั้งให้เองเฉพาะ repo นี้
- **เปลี่ยน oauthScopes = ต้องกด Allow ใหม่** ตอนเปิดเว็บครั้งแรกหลัง deploy
- **`clasp login` หมดอายุเงียบ ๆ** → push พังด้วย `invalid_grant / invalid_rapt`
  โค้ดไม่ได้ผิดอะไรเลย แค่ต้องล็อกอินใหม่ → ดับเบิลคลิก `เข้าสู่ระบบ clasp.bat`
- **🚨 "ขึ้นระบบสำเร็จ" ไม่เท่ากับ "เบียร์เห็นของใหม่"**
  v0.2.0 ขึ้นจริง ป้ายเวอร์ชันถูก แต่เบียร์บอกไม่เห็นอะไรเปลี่ยน เพราะ**ฐานข้อมูลว่าง ไม่มีเอกสารสักใบ**
  ของใหม่อยู่ "ข้างในใบเคลม" พอไม่มีใบให้เปิด หน้าที่เห็นจึงเหมือนเดิมเป๊ะ
  → ทำฟีเจอร์ที่อยู่ลึกในเอกสารเมื่อไหร่ **ต้องบอกให้ชัดว่าต้องมีข้อมูลอะไรก่อนถึงจะเห็น**
    และหน้าที่ยังไม่มีข้อมูลต้องมีข้อความ "เริ่มยังไงดี" เสมอ ห้ามปล่อยจอเปล่า
- **ชื่อฟังก์ชันซ้ำกันคนละไฟล์ .js = Apps Script เลือกตัวไหนก็ได้** พังแบบเดาไม่ได้
  ย้ายฟังก์ชันไปไฟล์ใหม่เมื่อไหร่ **ต้องลบตัวเก่าทิ้งทันที**

## 📏 กฎตัวเลขที่ห้ามลืม
- **เงิน = 2 ตำแหน่งเสมอ** ยอดทุกหน้าต้องเท่ากัน
- **จำนวน = ข้อมูลมายังไงตามนั้น ห้ามปัดเอง**
- **วันที่ = DD/MM/YYYY เสมอ** ทุกที่ ทั้งกรอก ทั้งแสดง ทั้งบันทึก
- **ต้นทุน / กำไร ห้ามหลุดไปถึง Supplier**

## 🌐 ที่อยู่จริงของระบบ (ขึ้นแล้ว 3 ก.ย. 2569)
| อะไร | ค่า |
|---|---|
| Apps Script id | `139gkd7Mph1nYIgyhe79tpNISsUlhuOjox7TcYOFAQHTtOKgxwRWBbOlR` |
| Deployment id (ห้ามเปลี่ยน) | `AKfycbyps-WDVmA_FkR76GQlL_wvEsVIL3j2ZTmamQGZMjjhM4CWYQAjhvNcfEioYbB_AsVVww` |
| ที่อยู่เว็บ | `https://script.google.com/macros/s/AKfycbyps-WDVmA_FkR76GQlL_wvEsVIL3j2ZTmamQGZMjjhM4CWYQAjhvNcfEioYbB_AsVVww/exec` |

⚠️ **`clasp deploy` ต้องใส่ `-i <deployment id>` เสมอ** ไม่งั้นได้ URL ใหม่ทุกครั้ง ลิงก์ที่แจกพนักงานจะตาย
(`อัปเดตระบบเคลม.bat` ใส่ให้แล้ว — อย่าไปแก้ออก)

## 🚀 วิธีขึ้นระบบ
- ครั้งแรกครั้งเดียว: ดับเบิลคลิก **`ติดตั้งครั้งแรก.bat`**
- ครั้งต่อ ๆ ไป: แก้ไฟล์ใน `deploy/` แล้วดับเบิลคลิก **`อัปเดตระบบเคลม.bat`**
  (ตรวจไวยากรณ์ → git commit+push → clasp push → clasp deploy · พังตรงไหนหยุดทันที ไม่อัปโหลดของเสีย)
- GitHub: `https://github.com/STT-WEB/Claim-Notice`

## 📁 ไฟล์ในโปรเจกต์
| ไฟล์ | ทำอะไร |
|---|---|
| `deploy/CLAIM-Hub.js` | หลังบ้านทั้งหมด (config · ผู้ใช้ · จ๊อบ · ฐานข้อมูล · เลขเอกสาร · ใบเคลม) |
| `deploy/CLAIM-Index.html` | โครงหน้าเว็บ + หน้าเข้าสู่ระบบ (ไฟล์เดียวที่มี `?>` ได้) |
| `deploy/styles.html` | CSS ทั้งหมด |
| `deploy/js-core.html` | เข้าสู่ระบบ · เมนู · เรียก server |
| `deploy/CLAIM-Inspect.js` | หลังบ้านใบตรวจรับ (แม่แบบเช็คลิสต์ · Acc/Un-Acc · ส่งข้อที่ไม่ผ่านไปเปิดใบเคลม) |
| `deploy/CLAIM-More.js` | หลังบ้านแท็บ 2-4 (คำตอบ Supplier · เงิน+เรทล็อก+ค่าแรง · รับของกลับ) + รายงาน 4 หน้า |
| `deploy/js-claim.html` | ใบเคลม 4 แท็บ + **ระบบรูปที่ใช้ร่วมกันทั้งใบเคลมและใบตรวจ** |
| `deploy/js-inspect.html` | หน้าตรวจรับสินค้า |
| `deploy/js-report.html` | รายงาน 4 หน้า (รูป · ตามจ๊อบ · ทะเบียน · ต้นทุน) |
| `_tests/syntax-check.js` | ตรวจก่อน push (เรียกอัตโนมัติจาก .bat) |
| `_tests/guard-v0100.js` | ชุดทดสอบกันพัง |
| `สเปคงานเคลม (ตรวจก่อนเริ่มเขียน).md` | **สเปคเต็มที่เบียร์ตรวจแล้ว — อ่านก่อนทำฟีเจอร์ใหม่** |
| `Mockup-ใบเคลม-ร่าง5.html` | หน้าตาแบบฟอร์มที่เบียร์เคาะ (ใช้เป็นต้นแบบตอนทำหน้าปริ้น) |

## 🔗 ข้อมูลกลางที่อ่าน (อ่านอย่างเดียว 100%)
| อ่านอะไร | จากไหน |
|---|---|
| ผู้ใช้ + PIN + สิทธิ์ | MASTER แท็บ `USERS` คอลัมน์ **`role for Claim`** |
| เลขจ๊อบ / ชื่อลูกค้า / รุ่น | MASTER แท็บ `All WIP JT/JM` |
| รายชื่อผู้ขาย | MASTER แท็บ `VENDORS` (หัวตารางแถว 2) |
| เลขใบส่งมอบ | TXN แท็บ `DELIVERY_NOTES` |
| วันรับสินค้าจากเลข PO | PO Report ปี 2563–2569 |

⚠️ **ห้ามอ่าน USERS ด้วยคำว่า `role` เฉย ๆ** — จะไปโดน `role for NOVA BOM` ต้องหา `role for claim` ก่อนเสมอ

## 📌 สิ่งที่ทำแล้ว / ยังไม่ทำ
ดู `deploy/CHANGELOG.md` บรรทัดแรก และหัวข้อ "แผนลงมือ" ในไฟล์สเปค
